import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('Process Worker Test', () => {
    fit('should add top level imports', async () => {
        expect(true).toEqual(true);
      }
    );
  }
);
